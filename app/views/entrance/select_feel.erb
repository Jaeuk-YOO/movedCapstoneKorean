<div class="container">
    <%if current_user.language == "1" %>
    <br>
    <div class="alert alert-warning" role="alert">
        오늘은 어떤 곳으로 떠나볼까요? 공간의 특성에 따라 가볼 곳을 추천해드립니다. <hr/ style="border: #9b9b9b solid 0.5px;">관심있는 카드는 오른쪽으로, 그렇지 않은 키워드는 왼쪽으로 버려주세요.
    </div>

    <h3 class="slider-title">feeling and feature</h3>
    <div class="slider">
        <div class="slide" ><img src="/assets/select_feel/Active2.png" /><p>Active</p></div>
        <div class="slide"><img src="/assets/select_feel/comfortable1.png" /><p>Comfortable/cosy</p></div>
        <div class="slide"><img src="/assets/select_feel/traditional2.png" /><p>Traditional</p></div>
        <div class="slide" ><img src="/assets/select_feel/Korean-activity3.png" /><p>Korean-activity</p></div>
    </div>


    <form action="/entrance/select_feel_post" method="POST">
        <div class="row">
            <%@select_list.each_with_index do |each_select_value, index|%>
            <div class="col-6 p-0 border border-white" style="border-width:5px!important;">
                <span class="p-auto" style="font-size:1rem; position:absolute; z-index:99; color:white; text-shadow: 2px 2px 6px black; padding-left: 35%; padding-top: 23%;"><%= each_select_value %></span>
                <img src="/entrance_images/<%=each_select_value%>.jpg" class="img-check" width="100%;" name="chk<%= index %>">
                <input type="checkbox" name="chk[]" id="chk<%= index %>" value="<%= each_select_value %>" style="display:none" autocomplete="off">
            </div>
            <%end%>
        </div>
        <br><input type="submit" value="선택하기" class="btn btn-block btn-warning">
    </form>
    &nbsp;<br>

    <%elsif current_user.language == "2" %>
    <br>
    <div class="alert alert-warning" role="alert">
        Where do you want to go today? We recommend places to visit depending on the characteristics of the space. Please select the information you are interested in.
    </div>
    <p class="feeling-title">feeling and feature</p>
    <form action="/entrance/select_feel_post" method="POST">
        <div class="row">
            <%@select_list_eng.each_with_index do |(each_select_key, each_select_value), index|%>
            <div class="col-6 p-0 border border-white" style="border-width:5px!important;">
                <span class="p-auto" style="font-size:1rem; position:absolute; z-index:99; color:white; text-shadow: 2px 2px 6px black; padding-left: 35%; padding-top: 23%;"><%= each_select_key %></span>
                <img src="/entrance_images/<%=each_select_value%>.jpg" class="img-check" width="100%;" name="chk<%= index %>">
                <input type="checkbox" name="chk[]" id="chk<%= index %>" value="<%= each_select_value %>" style="display:none" autocomplete="off">
            </div>
            <%end%>
        </div>
        <br><input type="submit" value="Select" class="btn btn-block btn-warning">
    </form>
    &nbsp;<br>
    <%end%>
<br>
</div>

<style>
.check
{
    opacity:0.5;
	color:#996;
}
</style>


<script>
$(document).ready(function(){
    $(".img-check").click(function(){
        $(this).toggleClass("check");
        var img_name = $(this).attr('name');

        if ($(this).attr('class') != "img-check check") {
            $("input:checkbox#"+img_name).attr("checked", false);
        }
        else if ($(this).attr('class') == "img-check check") {
            $("input:checkbox#"+img_name).attr("checked", true);
        }
    });
    $(".p-img-check").click(function(){
        $(this > ".img-check").toggleClass("check");
        var img_name = $(".img-check").attr('name');

        if ($(".img-check").attr('class') != "img-check check") {
            $("input:checkbox#"+img_name).attr("checked", false);
        }
        else if ($(".img-check").attr('class') == "img-check check") {
            $("input:checkbox#"+img_name).attr("checked", true);
        }
    });
});
</script>